A abstração de dados do meu sistema foi construída para traduzir o funcionamento real de uma oficina mecânica para uma lógica de programação estruturada, utilizando classes, objetos e métodos que organizam as informações e as ações do utilizador. 
A primeira estrutura central é a Classe Usuário, que eu utilizei para gerir a segurança. Cada vez que alguém faz login, o sistema cria um objeto usuário com atributos como e-mail e um ID exclusivo. Os métodos principais aqui são o de Autenticação, que valida os dados no Firebase, e o de Controle de Sessão, que garante que o sistema só mostre as informações se o utilizador estiver logado. 
Para a gestão dos clientes, criei a Classe Cliente. Cada cliente registado torna-se um objeto com atributos como nome, documento (CPF/CNPJ) e telefone. Nesta classe, os métodos que eu desenvolvi são fundamentais para a experiência do utilizador: o método de Máscara de Dados, que formata automaticamente o que é digitado, e o método de Integração com API, que captura o número de telefone do objeto e abre o link direto para o WhatsApp. 
A parte mais operacional do meu código é a Classe Ordem de Serviço (OS). Ela transforma o serviço da oficina num objeto digital com atributos como veículo, descrição, valor e status (pendente, finalizado, etc.). Desenvolvi métodos específicos para esta classe, como o de Atualização de Status, que permite mudar o estado do serviço no banco de dados sem recarregar a página, e o método de Geração de Documento, que mapeia todos os dados desse objeto para criar o ficheiro PDF de impressão. 
Para a inteligência do negócio, utilizei a Classe Dashboard. Esta classe não guarda apenas dados, ela processa-os. Através de métodos de Cálculo Financeiro, o sistema percorre todos os objetos de Ordens de Serviço para extrair o faturamento total e calcular o ticket médio. Outro método vital é o de Processamento Gráfico, que conta quantos serviços existem em cada status e envia esses valores para a biblioteca Chart.js desenhar o gráfico. 
Por fim, incluí uma Classe de Configuração, que gere os dados da própria oficina. Ela possui atributos como nome da empresa e endereço, e utiliza o método de Persistência Local (LocalStorage). Esse método é importante porque guarda as preferências no navegador do utilizador, permitindo que, quando eu gerar o PDF, o sistema saiba exatamente qual cabeçalho deve colocar, tornando o software flexível para qualquer oficina usar. 

 